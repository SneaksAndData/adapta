name: Validate code format

on:
  pull_request:
    branches: [ main ]

jobs:
  validate_code:
    name: Lint and format
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write
      pull-requests: write

    if: ${{ github.ref != 'refs/heads/main' }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - name: Set up Python
        run: uv python install

      - name: Lint
        # --output-format=github turns errors into clickable annotations
        run: uv run ruff check --output-format=github

      - name: Check Formatting with Ruff
        # --check fails the build if the code isn't perfectly formatted
        run: uv run ruff format --check